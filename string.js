// Store favorite actor's name
let favActorFirstName = "Coco";
let favActorLastName = "Martin";

// Concatenate first and last name
let fullName = favActorFirstName + " " + favActorLastName;

// Convert to uppercase
let uppercase = fullName.toUpperCase();

// Create a message
let message = `My favorite actor is ${uppercase}.`;

// Append additional message
message += " His best show is ang probinsyano.";

// Log all variables
console.log(fullName, uppercase, message);
